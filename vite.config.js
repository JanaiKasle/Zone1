import { defineConfig } from "vite";

// vite.config.js
export default defineConfig({
  // config options

  // For specifying sub-page when deploying to gh pages
  // https://vitejs.dev/guide/static-deploy.html#github-pages
  base: "/HongKong_Zoning/"
});
